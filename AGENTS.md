# Charkhoone engineering guardrails

این مخزن بر اساس سند فنی نسخه ۳ پلتفرم ملی چارخونه توسعه می‌یابد.

## قواعد غیرقابل حدس

- رتبه اعتباری از سامانه بیرونی می‌آید؛ چارخونه رتبه تولید نمی‌کند.
- فرمول رهن کامل: `ودیعه نقدی + (اجاره ماهانه / 0.03)`.
- درصد سقف وام گروه‌ها: A=55%، B=45%، C=40%، D=35%، E=30%.
- آورده مستأجر از وام تأییدشده بانک محاسبه می‌شود، نه صرفاً سقف نظری.
- اصل وام بانک در صندوق فریز است و برای پوشش تأخیر قابل برداشت نیست.
- سه ماه متوالی عدم پرداخت معیار ابطال است؛ پرداخت ماه جاری بدهی قبلی را خودکار تسویه نمی‌کند.
- روزشمار ۳٪، پرداخت جزئی، گردکردن و زمان تسویه اصل وام در ابطال زودهنگام تصمیم باز هستند. تا تصویب، برای آن‌ها رفتار تجاری اختراع نکنید.

## اتصال‌های بیرونی

API واقعی بانک، صندوق، سازمان، خودنویس و اعتبارسنجی هنوز قرارداد نهایی ندارد. adapterها پشت interface مستقل باشند و fake/mock فقط در محیط توسعه استفاده شود.

## معماری

- Modular Monolith
- API و Worker اجرای مستقل از یک codebase
- PostgreSQL منبع داده اصلی
- عملیات مالی idempotent و قابل ممیزی
- Outbox/Inbox و RabbitMQ در مرحله پردازش رویدادها
- UI فارسی/RTL و پیاده‌سازی صفحات از Figma پس از استخراج design context همان صفحه
